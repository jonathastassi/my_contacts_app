import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';

import 'package:my_contacts_app/app/modules/home/pages/initial/initial_controller.dart';
import 'package:my_contacts_app/app/modules/home/home_module.dart';

void main() {
  initModule(HomeModule());
  // InitialController initial;
  //
  setUp(() {
    //     initial = HomeModule.to.get<InitialController>();
  });

  group('InitialController Test', () {
    //   test("First Test", () {
    //     expect(initial, isInstanceOf<InitialController>());
    //   });

    //   test("Set Value", () {
    //     expect(initial.value, equals(0));
    //     initial.increment();
    //     expect(initial.value, equals(1));
    //   });
  });
}
