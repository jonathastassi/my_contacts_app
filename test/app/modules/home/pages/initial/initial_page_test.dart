import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:my_contacts_app/app/modules/home/pages/initial/initial_page.dart';

void main() {
  testWidgets('InitialPage has title', (tester) async {
    //  await tester.pumpWidget(buildTestableWidget(InitialPage(title: 'Initial')));
    //  final titleFinder = find.text('Initial');
    //  expect(titleFinder, findsOneWidget);
  });
}
