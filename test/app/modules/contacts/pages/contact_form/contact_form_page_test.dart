import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:my_contacts_app/app/modules/contacts/pages/contact_form/contact_form_page.dart';

void main() {
  testWidgets('ContactFormPage has title', (tester) async {
    //  await tester.pumpWidget(buildTestableWidget(ContactFormPage(title: 'ContactForm')));
    //  final titleFinder = find.text('ContactForm');
    //  expect(titleFinder, findsOneWidget);
  });
}
