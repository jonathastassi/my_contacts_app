import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:my_contacts_app/app/modules/contacts/pages/contact_list/contact_list_page.dart';

void main() {
  testWidgets('ContactListPage has title', (tester) async {
    //  await tester.pumpWidget(buildTestableWidget(ContactListPage(title: 'ContactList')));
    //  final titleFinder = find.text('ContactList');
    //  expect(titleFinder, findsOneWidget);
  });
}
